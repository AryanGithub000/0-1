//Aynsc await syntax
//(Again, just syntactic sugar. Still uses callbacks/Promises under the hood)
//makes code more readable than callbacks/promises

//instead of function we add a ASYNC DECLARATION to it at the start
//also uses AWAIT instead of .then
//
